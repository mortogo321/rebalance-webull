# Active colour: green. Copied to upstream.active.conf by switch.sh.
upstream web_active {
  server web-green:3000 max_fails=2 fail_timeout=5s;
  keepalive 16;
}
upstream api_active {
  server api-green:8000 max_fails=2 fail_timeout=5s;
  keepalive 16;
}
