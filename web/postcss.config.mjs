// Tailwind CSS v4: the PostCSS plugin moved to its own package, and import
// handling plus vendor prefixing are built in, so postcss-import and
// autoprefixer are no longer needed.
export default {
  plugins: ["@tailwindcss/postcss"],
};
